[package]
name = "aerowave-core"
version = "0.2.0"
edition = "2021"
license = "MIT"
description = "Parsing and scheduling rules, with no GUI dependencies so they can be unit tested"

[dependencies]
chrono = "0.4"
